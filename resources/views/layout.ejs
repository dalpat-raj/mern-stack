<!DOCTYPE html>
<html lang="en">
    <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Real Pizza</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css">
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <!-- navbar  -->
    <nav class="pore container mx-auto flex items-center justify-between py-4">
        <div class="logo">
            <img src="/img/logo.png" alt="logo">
        </div>
        <% if(user) { %>
            <div>
                <h3>Hello <%= user.name %></h3>
            </div>
        <% } %>
        <div>
            <ul class="navbarr flex items-center">
                <li class="ml-6"><a href="/">Home</a></li>
                <% if(user) { %>
                <% if(user.role === 'customer') { %>
                <li class="ml-6"><a href="/customer/orders">My Orders</a></li>
                <% } else { %>
                    <li class="ml-6"><a href="/admin/orders">Orders</a></li>
                <% } %>
                <li class="ml-6">
                    <form id="logout" action="/logout" method="POST">
                        <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                    </form>
                </li>
                <% } else { %>
                <li class="ml-6"><a href="/register">Register</a></li>
                <li class="ml-6"><a href="/login">Login</a></li>
                <% } %>
                <li class="ml-6"><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
                    <span id="cartCounter" class="text-white font-bold pr-2"><%= session.cart ? session.cart.totalQty : '' %></span>
                    <img src="/img/cart.png" alt="">
                </a></li>
            </ul>
        </div>
        <div class="humbger" id="humbger">
            <img src="/img/menu.png" alt="">
        </div>
        <div class="menu-close">
            <img src="/img/menu-close.png" alt="">
        </div>
    </nav>

    <%- body%>
    
    <div class="container mx-auto w-full md:w-1/4 text-center">
        <span class="text-xl font-bold mb-4 text-center">Author</span>
        <hr>
        <div class="author  text-center flex justify-between items-center">
            <div>
                <img class="w-25 h-40" src="/img/mera.png" alt="">
            </div>
            <div>
                <p class=" dalpat font-lg font-bold">Dalpat Raj</p>
                <p>MERN Stack Devloper</p>
            </div>
        </div>
    </div>

    <footer class="w-full px-6 py-6">    
            <hr> 
            <div class="footer-2 text-center">
                    <p class="text-lg">All Right Reserved </p>
                <div class="fftext">
                    <p class="text-sm">Conditions of Use & SalePrivacy NoticeInterest-Based Ads© 1996-2021, Amazon.com, Inc. or its affiliates</p>
                </div>
            </div>  
    </footer>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>

</body>
</html>